//import { expect } from "chai";
//import { connect, clear, close } from '../../db'
//import { newPhotographers } from '../../seed'
//import { IPhotographer } from "../types/photographer";
//import Photographer from "../../models/photographer";


//describe('...', () => {
  //before(async () => await connect());
  //beforeEach(async () => await clear())
  //after(async () => await close());

  //it('...', async () => {
    //const photographer: IPhotographer = new Photographer({
      //name: newPhotographers[0].name,
      //availabilities: newPhotographers[0].availabilities,
      //bookings: newPhotographers[0].bookings,
    //});

    //try {
      //await photographer.save()
    //} catch (error) {
      //console.log('selam', error)
    //}

    //});
//});
